# Example .env for development
# create a .env file in the repo root and set values there

# PORT to run server on (default 8080)
PORT=8080

# Optional: one of
# - a hosted Postgres / Neon connection string (recommended if you want DB)
# - a local development DB
# Example PostgreSQL URL (replace with your credentials):
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname
